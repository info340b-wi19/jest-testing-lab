'use strict';

import {greet, invertCase, selectionSort} from './js/app'; //import the functions to test!

// Make a basic set of math and logic tests and group them all into one using describe(..)!
    
    // Check to see that 5+5 = 10 using .toEquals
    
    // Check to see if an array contains a nonexistent value using .not

    // Check to see if 2 arrays are equal


// Make a category of tests using describe for the greeting function in app.js

    // Check to make sure the greet function in app.js returns the correct output


// Make a category of tests using describe for the invertCase function in app.js

    // Make a basic test to see if it returns the correct output in a 1 letter input

    // Check to see that it inverts the case the other way around from what you tested initially

    // Write a test to make sure it inverts multiple letters

    // Check to make sure it correctly doesn't invert non-letters


// Make a category of tests using describe for the selectionSort function in app.js

    // Write your own tests and try to fix the function!